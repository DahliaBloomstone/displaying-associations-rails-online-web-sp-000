<h1><%= @post.title %></h1>

<!-- Add code here to link to the post's category. -->

<h3>Category: <%= link_to @post.category.name, category_path(@post.category) if @post.category %></h3>

<!-- heavy use of associations. -->
<h1><%= @category.name %></h1>

<h3><%= pluralize(@category.posts.count, 'Post') %></h3>
<ul>
 <% @category.posts.each do |p| %>
   <li><%= link_to p.title, post_path(p) %></li>
 <% end %>
</ul>

<p><%= @post.description %></p>


<!-- @post.category is the Category model itself
we can use it anywhere we would use @category in a view for that object.
-->

<!-- Post.count
 => 4
clickbait = Category.find_by(name: "Motivation")
 => #<Category id: 1, ...>
clickbait.posts.count
 => 3 -->
